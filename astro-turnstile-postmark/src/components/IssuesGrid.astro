---
const issues = ['Collections','Bankruptcies','Judgments','Repossessions','Liens','Charge Offs','Foreclosures','Late Payments'];
---
<section class="py-12 sm:py-16">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <h2 class="text-2xl sm:text-3xl font-bold tracking-tight text-slate-900 text-center">Items we can dispute</h2>
    <div class="mt-8 grid grid-cols-2 sm:grid-cols-4 lg:grid-cols-8 gap-3 text-center text-sm">
      {issues.map((x) => (
        <div class="rounded-lg border border-slate-200 bg-white px-3 py-2 shadow-sm">{x}</div>
      ))}
    </div>
  </div>
</section>
